#!/usr/bin/env python
# Check if Vagranfile exists in the current directory
# Create a tmp directory if it exists delete recreate the dir
	# Add a command line flag to not delete tmp
# Get all minion hostnames as command line arguments
# Create minion specific files under tmp directory
	# Create /etc/hosts file under each minion directory
# Set a private IP for salt master

